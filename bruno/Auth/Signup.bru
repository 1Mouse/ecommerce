meta {
  name: Signup
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/auth/signup
  body: json
  auth: none
}

body:json {
  {
    "name": "{{authName}}",
    "email": "{{authEmail}}",
    "password": "{{authPassword}}"
  }
}
