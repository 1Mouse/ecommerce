meta {
  name: Upload My Image
  type: http
  seq: 3
}

put {
  url: {{baseUrl}}{{usersUploadMyImagePath}}
  body: multipart-form
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:multipart-form {
  image: @file(/path/to/profile.png)
}
